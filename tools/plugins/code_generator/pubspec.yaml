name: code_generator
description: A Flutter code generator plugin for generating boilerplate code
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  build_runner: ^2.4.0
  source_gen: ^1.3.0
  build: ^2.4.0

dev_dependencies:
  test: ^1.24.0
