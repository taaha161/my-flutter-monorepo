name: linter_rules
description: Custom linter rules for the Flutter monorepo
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^6.0.0
  meta: ^1.11.0

dev_dependencies:
  test: ^1.24.0
